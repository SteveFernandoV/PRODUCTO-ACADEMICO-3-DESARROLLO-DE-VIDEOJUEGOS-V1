# PA3 – Matriz de avance y evidencias (Unity)

Actualizado: 22/09/2026. Estado: flujo reestructurado para menú independiente y escena de juego; se conserva el espacio de terreno para diseño del estudiante. La escena `PA3_MainMenu` está configurada para generarse desde el Editor al volver a Edit Mode; falta confirmar su guardado visual en Unity. Esta matriz registra presencia estructural; “implementado” no equivale a criterio aprobado ni a prueba visual/play mode.

Limpieza 22/09/2026: siete recursos `PA3_*` sin referencias serializadas se retiraron de `Assets` y sus copias de materiales fueron eliminadas. Se conservaron `New Terrain.asset`, `New Terrain 1/2/3.asset`, `NewLayer.terrainlayer`, los paquetes AllSky/Polytope, prefabs, shaders y Timeline. El Editor confirmó `PA3_TERRAIN_TEMPLATE_READY` y `PA3_BUILD_OK`. Permanece una escena antigua en `PA3_Backup_Source/PreEnvironmentEnhancement` porque el entorno bloqueó su eliminación directa; no se incluye en Unity Project Browser.

## Auditoría inicial

- Proyecto: PRODUCTO ACADEMICO 2 DESARROLLO DE VIDEOJUEGOS.
- Unity 6000.7.0b1 (Beta); URP 17.7.0; Input System 6.7.0; Cinemachine 3.1.7; Timeline 6.7.0; AI Navigation 2.0.14.
- Escena original `Assets/Scenes/SampleScene.unity` preservada y todavía incluida en Build Settings. Nueva escena `Assets/Scenes/PA3_VerticalSlice.unity` también incluida.
- Git existe; el proyecto ya tenía cambios locales previos. No se restablecieron ni eliminaron esos cambios.
- Flujo objetivo: `PA3_MainMenu` como primera escena de Build Settings → botón JUGAR carga `PA3_VerticalSlice` → portal tras los seis cristales devuelve al menú con mensaje de victoria → captura del Guardián devuelve al menú con mensaje de derrota. El menú anterior `Assets/Scenes/menu.unity` era una plantilla vacía con `New Text`; el generador lo retira después de crear la escena nueva. `SampleScene` se conserva como archivo original pero se excluye del Build.
- ProBuilder se probó, pero su paquete no compila en esta versión beta de Unity (errores de API obsoleta en TreeView/BinaryFormatter); se retiró del manifest para recuperar compilación limpia. No afirmar uso de ProBuilder.
- Registro de construcción: `Logs/Editor.log` contiene `PA3_BUILD_OK`; al cierre, 0 errores C# y 0 excepciones recientes. En Console visual, contadores de logs/avisos/errores en 0.
- Incidencias reales encontradas y corregidas en la revisión visual: referencias de scripts faltantes por agrupar varios MonoBehaviours en `PA3Runtime.cs`; el controlador usaba `UnityEngine.Input` aunque el proyecto solo admite Input System; el modelo inicial era una cápsula sin silueta humana.
- Correcciones aplicadas: un archivo `.cs` por componente; el jugador usa `Keyboard.current` del Input System; modelo humanoide low-poly con cabeza, torso y extremidades; cámara apuntada al jugador desde el inicio y seguimiento más cercano; HUD con UI Text integrada, sin exigir importar TMP Essentials.

## Matriz de estado

| Bloque / requisito | Estado | Evidencia en proyecto | Pendiente de validación |
|---|---|---|---|
| Auditoría, URP y organización | Implementado | Packages/manifest.json; Assets/PA3/{Editor,Scripts,Shaders,Materials,Prefabs,Timeline,Textures}; escena PA3 en EditorBuildSettings | Abrir GUI, Console visual y confirmar resolución de paquetes en Package Manager |
| Terreno, escenario 3D y verticalidad | Pendiente de diseño del estudiante | Escena `PA3_VerticalSlice.unity` reconstruida; contenedor `YOUR_TERRAIN_AND_LEVEL_DESIGN_HERE` vacío. Registro: `PA3_TERRAIN_TEMPLATE_READY`, `PA3_BUILD_OK` | Crear Terrain/ProBuilder según disponibilidad y diseñar las bases/verticalidad; ProBuilder no disponible en esta versión beta |
| Ambientación con assets descargados | Assets listos, sin decoración automática | Prefabs fuente de árboles, rocas, pasto y flores permanecen en `Assets/Polytope Studio`; el constructor deja libre la escena para ubicarlos sobre el terreno propio | Arrastrar y distribuir assets sobre el terreno del estudiante y validar Game View |
| Control 3D y cámara | Parcial, prefabs rediseñados | `Player_3D.prefab` con armadura, visor, mochila, detalles luminosos y punto de spawn propio; `PA3_CameraRig.prefab` sigue automáticamente al objeto Player etiquetado. Se conserva movimiento/salto Input System | Validar recorrido y saltos con terreno propio en Play Mode |
| Interacción Trigger/Collider y objetivo | Parcial; portal restaurado en la escena, pendiente validar en Editor | `PA3_VerticalSlice.unity` ahora contiene una instancia del prefab `PA3_ExitPortal` en (30, 0, 22) y mantiene `Exit_Trigger_TEMPLATE` activo, invisible y con `ExitTrigger`; existe además la orden idempotente de reparación desde el menú PA3 del Editor. | Reabrir/recargar la escena para que Unity importe el cambio; verificar el portal visualmente y probar bloqueo por cristales, victoria y captura en Play Mode |
| Menú principal y navegación | Implementado en código de Editor/runtime; falta confirmar generación visual | `PA3SceneBuilder` genera `PA3_MainMenu` con botones JUGAR, CÓMO JUGAR y SALIR; es primera en Build Settings. JUGAR abre `PA3_VerticalSlice`; portal/captura retornan al menú mostrando resultado. Se retira la vieja escena de prueba `menu.unity` tras generar la nueva; `SampleScene` queda archivada pero fuera del flujo | Salir de Play Mode para que Unity compile y ejecute el generador; confirmar botones y cambios en Build Settings. No declarar probado hasta ejecutar cada transición |
| Trail del jugador (tutorial, 02:53:42) | En preparación, prefab-only | El generador añade `TrailRenderer` cian-violeta, corto y luminoso en el jugador; se integrará en `Player_3D.prefab` | Falta regenerar el prefab y comprobarlo moviendo/saltando en Play Mode; no se ha reemplazado ningún jugador de la escena |
| VFX / partículas | Prefab independiente actualizado; verificación visual pendiente | `Assets/PA3/Prefabs/CrystalMotesVFX.prefab`: motas cian–dorado de baja densidad, deriva ascendente/orbital y turbulencia suave (máx. 96; 14 por segundo). La copia fija anterior de la escena quedó inactiva para que puedas colocar el prefab donde quieras. Constructor alineado en `PA3SceneBuilder.CreateVfx`. | Confirmar importación del prefab en Unity y comprobar aspecto/rendimiento en Game view; aún no se declara el criterio probado |
| Shader Graph dinámico | Implementado; falta capturar evidencia del pulso | `Shaders/DynamicWorld.shadergraph` conecta Time (Sine Time) → Add (+1,2) → Multiply con color `Diffuse` → Emission URP Lit. `Materials/PA3_Crystal_Glow.mat` usa el Shader Graph y está referenciado por los cristales de la escena; se observó en Game View sin material rosa. Console: 0 errores, 5 avisos ajenos del paquete Polytope/URP. | Grabar unos segundos de un cristal cercano para mostrar la variación temporal de emisión y tomar captura del grafo abierto. |
| Cinemachine + Timeline 5–10 s | Implementado y probado en Play Mode (24/09/2026) | `Timeline/PA3_Cinematic_7_5s.playable`: tres Cinemachine Shot Clips de 2,5 s (bosque, cristal, portal), duración fija 7,5 s; `Cinematic_Director` enlazado a Cinemachine Brain; la cámara de seguimiento y los controles se restauran al terminar. En Game View se observó la intro y el retorno al jugador; Unity registró `PA3_CINEMATIC_READY`. | Guardar una captura o vídeo breve del Timeline y de los tres planos para la entrega; ajustar encuadres si se desea. El aviso gráfico de Polytope/URP observado es ajeno a esta función. |
| Iluminación mixta + Post Processing | Implementado y ajustado suavemente | Lightmap y `LightingData.asset` generados en `Assets/Scenes/PA3_VerticalSlice`; `PA3_PostProcessing_Profile.asset` con Bloom (umbral 0,8; intensidad 0,5), exposición +0,18, contraste +10, saturación +6 y viñeta 0,15. Verificado en Game View; no se modificó el Terrain ni se rehizo el bake. | Captura final del Game View; revisar por separado los avisos de shaders Polytope/URP si afectan la vegetación. |
| Prefabs y colaboración Git | Parcial | Prefabs de jugador, cámara, cristales, guardián, triggers, portal, VFX, beacon y luces se conservan como kit. El menú pasa a ser escena dedicada, no prefab duplicado. Proyecto con Git existente; no se creó commit | Confirmar conversión del guardián a instancia prefab y portal en la escena; colocar y probar sobre el terreno del estudiante; coordinar ramas/commits con el equipo |
| Console y QA | Estructura recompilada; revisión visual limpia | `Logs/Editor.log`: PA3_BUILD_OK; cinco prefabs sin `m_Script` nulo; Console observada con contadores 0/0/0 | Console estaba limpia durante la prueba; queda una pasada manual completa de movimiento e interacciones |

## Evidencias que faltan para cerrar criterios

1. Guardar captura final del Game view y de Scene con terreno, verticalidad, jugador, props, iluminación y VFX.
2. Prueba Play Mode completa: abrir desde `PA3_MainMenu`, JUGAR, mantener WASD/salto, recoger seis cristales, portal y salida por atrapada con retorno al menú; comprobar que ambas rutas reinician el estado.
3. Captura o vídeo de evidencia de la cinemática de 7,5 s ya implementada y observada en Play Mode.
4. Verificación visual del Shader Graph personalizado URP y su lógica dinámica (hoy existe un shader HLSL equivalente como implementación provisional).
5. Captura de iluminación con bake y del Post Processing activo; Console sin errores/warnings relevantes.
6. Video-defensa conforme al tiempo requerido por el documento oficial y entrega Git coordinada.

No se asigna puntaje ni se declara ningún criterio de rúbrica como cumplido todavía: el jugador y HUD ya son visibles y Console quedó limpia, pero faltan pruebas completas de gameplay y varios requisitos visuales/técnicos.
